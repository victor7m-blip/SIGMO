export default function Placeholder({ title }) {
  return (
    <section className="panel">
      <h2>{title}</h2>
      <p>Este módulo será desenvolvido na próxima etapa do SIGMO.</p>
    </section>
  )
}